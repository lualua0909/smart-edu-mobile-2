import React from 'react'

import { View } from 'react-native'

const MyOfflineSchedule = ({ route }) => {
    //   const {userId} = route.params;

    return (
        <View
            style={{
                flex: 1,
                backgroundColor: '#fff',
                marginBottom: 20
            }}></View>
    )
}

export default MyOfflineSchedule
